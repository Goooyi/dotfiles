#scroling speed
set -g @scroll-speed-num-lines-per-scroll 0.5
#set-option -sa terminal-overrides ",xterm*:Tc"
set -g mouse on

# specific setting for macOS users to handle pbcopy wrapper for yank to clipboard
set -s set-clipboard on

# option to enable image preview in yazi
# set -g allow-passthrough on
set -ga update-environment TERM
set -ga update-environment TERM_PROGRAM

# unbind C-b
# set -g prefix C-Space
# bind C-Space send-prefix

# Vim style pane selection
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Start windows and panes at 1, not 0
set -g base-index 1
set -g pane-base-index 1
set-window-option -g pane-base-index 1
set-option -g renumber-windows on

# Use Alt-arrow keys without prefix key to switch panes
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

# Shift arrow to switch windows
bind -n S-Left  previous-window
bind -n S-Right next-window

# Shift Alt vim keys to switch windows
bind -n M-H previous-window
bind -n M-L next-window

# set -g @catppuccin_flavour 'mocha'

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'christoomey/vim-tmux-navigator'
# set -g @plugin 'dreamsofcode-io/catppuccin-tmux'
# set -g @plugin 'thesast/tmux-transient-status'
set -g @plugin 'tmux-plugins/tmux-yank'

run '~/.tmux/plugins/tpm/tpm'

# set vi-mode
set-window-option -g mode-keys vi
# keybindings
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi C-v send-keys -X rectangle-toggle
bind-key -T copy-mode-vi y send-keys -X copy-selection-and-cancel

# Stay in copy mode after mouse selection
unbind -T copy-mode-vi MouseDragEnd1Pane
bind-key -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-selection

bind '"' split-window -v -c "#{pane_current_path}"
bind % split-window -h -c "#{pane_current_path}"

# ----------------------
# Status Bar
# -----------------------
# delete windows size parameters is right status
# set -g status-right " %H:%M %d-%b-%y"
# transparent statusbar colors
#――――――――――――――――――――――――――――――――
# set -g status-bg default
# set-option -g status-style bg=default
# set-option -g status-fg colour240
# set-window-option -g window-status-style fg=magenta

# Switch to previous session with Ctrl + Shift + Left
bind-key -n C-S-Left switch-client -p

# Switch to next session with Ctrl + Shift + Right
bind-key -n C-S-Right switch-client -n
